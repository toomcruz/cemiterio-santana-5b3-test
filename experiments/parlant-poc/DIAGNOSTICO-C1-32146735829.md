# Diagnostico C1 — run 32146735829

Commit base: `620d282`. Modelo: `gemini-3.1-flash-lite`. Uma conversa, um turno.

## Estado do run

| Etapa | Resultado |
|---|---|
| checkout / setup-python / dependencias | success |
| **Pre-flight (uma chamada ao modelo)** | **success em 5s — sem 404, sem 429** |
| Conversas reais contra a POC completa | **em execucao desde 14:11:43, sem transicao** |
| Publicar relatorio | nao alcancado |

Passou de 100 minutos com `timeout-minutes: 60` declarado, sem artefato
publicado e com os logs retornando 404 (a API so os libera no fim). Nao
cancelei: ja errei assim antes, agindo sobre status possivelmente defasado.

## O que a evidencia offline diz

O pre-flight prova que a chave, o modelo e a rede estao bons. O que trava esta
depois — e da para medir sem gastar cota.

### 1. O modelo da POC cai no RPM mais conservador

`agent/nlp.py`:

```python
DEFAULT_RPM_BY_MODEL = {
    "gemini-2.5-flash": 5,
    "gemini-3.7-flash": 10,
    "gemini-3.6-flash": 10,
    "gemini-3.5-flash": 10,
}
DEFAULT_RPM = 5
```

`gemini-3.1-flash-lite` **nao esta na tabela**. Cai no `DEFAULT_RPM = 5`, e o
`RateLimiter` espaca em `60/5 = 12 segundos` por chamada — serializado, para
geracao e embedding.

### 2. Quantas chamadas a POC atual precisa

Medido com o provider sintetico (zero rede, zero cota), dois pontos:

| Execucao | Turnos | Chamadas de geracao |
|---|---|---|
| 1 conversa | 3 | 801 |
| 300 conversas | 1059 | 17768 |

Ajuste linear entre os dois pontos:

```
17768 - 801 = 16967 chamadas   para   1059 - 3 = 1056 turnos
  -> marginal ~= 16,1 chamadas por turno
  -> fixo     ~= 801 - 3 x 16,1 ~= 753 chamadas de inicializacao
```

### 3. O produto das duas coisas

`753 chamadas x 12 s ~= 2h30min` **so para indexar o agente**, antes do primeiro
turno. O job morre no timeout de 60 minutos muito antes disso.

Ordem de grandeza, nao cronometro: o numero fixo vem do caminho sintetico, que
nao reproduz chamada a chamada o caminho real. Mas a conclusao nao depende da
precisao — mesmo metade disso ja estoura o timeout.

### 4. Por que so agora

A POC cresceu nesta fase, e o custo de indexacao cresce com ela:

| | Antes | Agora |
|---|---|---|
| Guidelines | 14 | 20 |
| Tools | 5 | 19 |
| Canned responses | 7 | 9 |

O run 32069767929 completou 5 conversas dentro do timeout com a POC menor. O
contrato novo — que resolveu o `<<__missing__>>` — trocou um problema de
inferencia por um problema de escala de indexacao.

## Conclusao

**A causa provavel nao esta no contrato das tools.** O tool calling nem chegou a
ser exercitado: o run nao passou da inicializacao. Classificar em A, B, C ou D
agora seria inventar — nenhuma das quatro descreve "o agente nao terminou de
subir".

## O que NAO foi feito (aguardando autorizacao)

Nenhuma correcao foi aplicada. Os dois caminhos obvios, para quando houver
decisao:

1. **`POC_GEMINI_RPM` no workflow.** A variavel ja existe e sobrescreve o RPM.
   Com chave paga, subir para o RPM real corta o tempo proporcionalmente. E uma
   linha de env, sem tocar em codigo.
2. **Cache de indexacao por `release_id`** — exatamente o que a Fase 8 do plano
   ja preve (`PARLANT_HOME/<release_id>`, construir -> indexar -> aquecer ->
   ativar). Hoje o `PARLANT_HOME` e limpo a cada execucao de proposito, para nao
   congelar a journey; com release imutavel, o cache passa a ser seguro e a
   indexacao deixa de se repetir.

A instrumentacao de captura do ToolCaller continua instalada e nao custa nada
quando o run finalmente passar da inicializacao.
