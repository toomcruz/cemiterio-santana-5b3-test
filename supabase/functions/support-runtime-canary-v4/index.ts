// Deployment wrapper: the executable source remains under edge-functions so
// the canary bundle is traceable to the V4 branch and runtime commit.
import "../../../edge-functions/support-runtime-canary-v4/index.ts";
